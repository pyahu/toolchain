# Pyahu Community — Go overlay (MISE_ENV=go)
# Go toolchain, linter, debugger, live reload, and container image builder.

[tools]
go              = "1.24"
"golangci-lint" = "2"
"go:github.com/go-delve/delve/cmd/dlv" = "latest"   # debugger (dlv)
air             = "1"      # live reload
ko              = "0.18"   # container images for Go
